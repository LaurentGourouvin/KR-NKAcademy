# Services

This folder contains modules responsible for external interactions:
API calls, database access, storage operations, authentication, and any I/O-related logic.

Services fetch or send data — they don't contain business logic or UI code.
